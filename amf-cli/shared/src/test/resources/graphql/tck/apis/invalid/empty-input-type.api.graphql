schema {
    mutation: Mutation
    query: Query
}

type Query {
    name: String
}

type Mutation {
    changeUserStatus(input_: InputType!): OutputType
}

input InputType {
    # this is empty
}

type OutputType {
    success: Boolean
}