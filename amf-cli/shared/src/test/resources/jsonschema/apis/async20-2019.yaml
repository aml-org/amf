asyncapi: 2.0.0
info:
  title: Api that uses Json Schema Document
  version: "1.0"
channels:
  /something:
    subscribe:
      message:
        payload:
          $ref: ../schemas/simple-2019.json#/$defs/Person
