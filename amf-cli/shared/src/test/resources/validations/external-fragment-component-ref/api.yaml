openapi: 3.0.0
info:
  version: 1.0.0
  title: test-refs-investigations
paths:
  /somePath:
    post:
      parameters:
        - $ref: pseudo-components.yaml#/components/parameters/aparameter
      responses:
        "200":
          description: test
          content:
            application/json:
              schema:
                type: string
