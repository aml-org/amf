asyncapi: 2.0.0
info:
  title: API
  version: "1.0"
channels:
  users/signup:
    subscribe:
      message:
        schemaFormat: application/raml+yaml;version=1.0
        payload:
          $ref: external-json.json#/nestedContent/User
