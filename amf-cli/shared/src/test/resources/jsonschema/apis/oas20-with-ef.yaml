swagger: "2.0"
info:
  title: Api that uses Json Schema Document
  version: "1.0"
paths:
  /something:
    get:
      description: something
      responses:
        200:
          description: something else
          schema:
            $ref: ../schemas/external-fragment.json#/$defs/People
