asyncapi: 2.0.0
info:
  title: My Invalid API
  version: '1.0.0'
channels:
  mychannel:
    publish:
      message:
        schemaFormat: application/vnd.apache.avro;version=1.9.0
        payload:
          $ref: 'invalid-schema/asyncapi-avro-invalid.json'
