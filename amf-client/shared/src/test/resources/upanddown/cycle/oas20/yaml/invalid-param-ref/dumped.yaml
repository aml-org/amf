swagger: "2.0"
info:
  title: asd
  version: asddd
paths:
  /resource:
    get:
      parameters:
        -
          $ref: "#/parameters/#/para"
      responses: {}
parameters:
  p1:
    in: query
    type: string
    name: p1
  p2:
    in: query
    type: string
    name: p2
