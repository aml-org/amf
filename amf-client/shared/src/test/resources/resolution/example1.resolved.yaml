swagger: "2.0"
paths:
  /resource:
    post:
      consumes:
        - application/json
      parameters:
        -
          name: OperationFormParam
          description: some body parameter
          type: string
          in: formData
      responses: {}
info:
  title: API
  version: "1.0"
