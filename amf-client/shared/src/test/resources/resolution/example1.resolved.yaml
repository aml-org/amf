swagger: "2.0"
consumes:
  - application/json
produces:
  - application/json
  - application/xml
paths:
  /resource:
    post:
      consumes:
        - application/json
      parameters:
        -
          name: OperationFormParam
          description: some body parameter
          type: string
          in: formData
      responses: {}
info:
  title: API
  version: "1.0"
