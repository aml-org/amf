#%Dialect 1.0

version: 1.0

external:
  schema-org: http://schema.org/

documents:
  root:
    encodes: MovieNode

nodeMappings:
  MovieNode:
    classTerm: schema-org.Movie
    mapping:
      name:
        propertyTerm: schema-org.name
        mandatory: true
        range: string
