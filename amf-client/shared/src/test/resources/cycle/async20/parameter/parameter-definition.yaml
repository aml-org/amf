asyncapi: '2.0.0'
info:
  title: components
  version: '1.0.0'

components:

  parameters:
    userId:
      description: id of the user
      location: "$message.payload#/user/id"
      schema:
        "$ref": "#/components/schemas/simple-obj"

  schemas:
    simple-obj:
      type: object



channels: {}
