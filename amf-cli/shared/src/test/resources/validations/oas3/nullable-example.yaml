openapi: 3.0.0
info:
  title: test oas 3.0 api
  version: 1.0.0
paths:
  /test:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: string
                example: null # should be a valid example
                nullable: true
