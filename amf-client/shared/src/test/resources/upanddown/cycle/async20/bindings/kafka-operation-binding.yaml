asyncapi: 2.0.0
info:
  title: Kafka operation binding
  version: 1.0.0
channels:
  some-channel:
    publish:
      bindings:
        kafka:
          groupId:
            type: string
            enum:
              - myGroupId
          clientId:
            type: string
            enum:
              - myClientId
