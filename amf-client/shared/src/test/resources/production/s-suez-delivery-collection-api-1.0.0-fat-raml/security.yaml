 - basic:
      description: Weighman API Supports BASIC for authenticating all API requests.
      type: Basic Authentication
      describedBy:
        headers: 
          Authorization: 
            description: Unencrypted Base64 encoded username and password.
            type: string
# - oauth_2_0:
#      description: Customer Portal API Supports OAuth 2.0 for authenticating all API requests.
#      type: OAuth 2.0
#      describedBy:
#        queryParameters:
#          access_token:
#            description: Used to send a valid OAuth 2 access token.
#            type: string
#        responses:
#          401:
#            description: Bad or expired token. This can happen if the user or Suez UK revoked or expired an access token. To fix, re-authenticate the user.
#          403:
#            description: Bad OAuth request (wrong consumer key, bad nonce, expired timestamp...)
#      settings:
#        authorizationUri: http://{http.host}:{http.port}/api/customerportal/{version}/oauth2/authorize
#        accessTokenUri: http://{http.host}:{http.port}/api/customerportal/{version}/oauth2/token
#       authorizationGrants: [ code, token, owner, credentials ]