openapi: 3.0.3
info:
  title: API
  version: 1.0.0

paths:
  /customers:
    get:
      parameters:
        - in: query
          name: enterpriseCustomerNumber
          schema:
            nullable: true
            type: string
      responses:
        '200':
          description: OK