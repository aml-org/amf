openapi: 3.1.0
info:
  title: API
  version: 1.0.0

paths:
  /something:
    parameters:
      - in: path
        name: invalid#param
        schema:
          type: string
        required: true
    get:
      parameters:
        - in: path
          name: invalid#param
          schema:
            type: string
          required: true
