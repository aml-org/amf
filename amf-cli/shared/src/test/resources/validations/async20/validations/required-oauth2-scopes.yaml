asyncapi: 2.0.0
info:
  title: aTitle
  version: aVersion
channels: {}
components:
  securitySchemes:
    some:
      type: oauth2
      flows:
        implicit:
          authorizationUrl: something
        password:
          tokenUrl: something
          authorizationUrl: something
        clientCredentials:
          tokenUrl: something
        authorizationCode:
          tokenUrl: something
          authorizationUrl: something