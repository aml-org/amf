{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "description": "A real life country. Uniquely identified by its two-letter (alpha-2) ISO 3166-1 country code.",
    "required": [
        "id"
    ],
    "allOf": [
        {
            "$ref": "identifier"
        }
    ],
    "properties": {
        "id": {
            "description": "The unique ISO 3166-1 alpha-2 code for this country.",
            "type": "string"
        },
        "name": {
            "description": "The name of this country in its own language.",
            "type": "string"
        }
    }
}
