schema {
    query: Query
}

type Query {
    text: String
}

type Person implements HasName {
    name: String!
    surname: String!
}

type Dog implements HasName {
    name: String!
    breed: String!
}

interface HasName {
    name: String!
}