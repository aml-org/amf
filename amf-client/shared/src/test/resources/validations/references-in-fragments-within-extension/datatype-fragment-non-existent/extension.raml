#%RAML 1.0 Extension
extends: api.raml
title: OpenId Connect No Resource Template (Override Me)

uses:
  openid: library.raml

types:
  DataType: !include dataType.raml

/endpoint:
  get:
    body:
      application/json:
        type: DataType