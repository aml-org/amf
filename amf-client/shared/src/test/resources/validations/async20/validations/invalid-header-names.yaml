asyncapi: 2.0.0
info:
  title: Something
  version: 1.0.0
channels:
  /test:
    publish:
      message:
        bindings:
          http:
            headers:
              type: object
              properties:
                Valid-Name:
                  type: string
                Invalid Name:
                  type: string
            bindingVersion: '0.1.0'