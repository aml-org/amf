swagger: '2.0'

responses:
  default:


    schema:
      type: string

paths:
  /resource:

    get:
      responses:

        400:

          description: default
        default:
          $ref: "#/responses/default"