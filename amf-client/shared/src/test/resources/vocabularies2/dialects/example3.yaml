#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  nodeB:
    classTerm: v2.A
    mapping:
      za:
        propertyTerm: v2.a
        range: nodeA
  nodeA:
    classTerm: v2.A
    mapping:
      a:
        propertyTerm: v2.a
        range: string
documents:
  library:
    declares:
      bs: nodeB
      as: nodeA
uses:
  v2: vocab2.yaml
