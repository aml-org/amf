#%RAML 1.0
title: Test
/unimplemented-post:
  get:
    queryParameters:
      param:
        required: false
        default: Default value
/implemented-post:
  get:
    queryParameters:
      param:
        required: false
        default: Default value
  post:
    body:
      application/json:
        type: string
