subscribe:
  operationId: "baseOperationId"
  bindings:
    http:
      bindingVersion: baseVersion
      type: baseVersion
      query:
        type: object
        properties:
          some:
            type: string
          another:
            type: object
            properties:
              aProperty: string