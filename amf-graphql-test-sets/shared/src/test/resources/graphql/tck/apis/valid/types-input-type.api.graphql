schema {
    mutation: Mutation
    query: Query
}

type Query {
    name: String
}

type Mutation {
    changeUserStatus(input_: InputType!): OutputType
}

input InputType {
    randomText: String
}

type OutputType {
    success: Boolean
}