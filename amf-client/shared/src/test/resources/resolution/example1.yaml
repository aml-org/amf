swagger: '2.0'

consumes:
  - application/json

produces:
  - application/json
  - application/xml

paths:
  /resource:
    post:
      parameters:
        - name: OperationFormParam
          in: formData
          description: some body parameter
          type: string