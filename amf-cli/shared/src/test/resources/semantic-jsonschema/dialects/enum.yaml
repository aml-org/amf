#%Dialect 1.0
nodeMappings:
  Person:
    mapping:
      name:
        range: string
        mandatory: false
        enum:
          - A
          - "5"
          - "false"
external:
  ns0: http://test.com/vocab#
dialect: amf-json-schema-generated-dialect
version: "1.0"
documents:
  root:
    encodes: Person
