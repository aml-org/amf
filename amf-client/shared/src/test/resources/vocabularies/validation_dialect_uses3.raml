#%RAML 1.0 Dialect

dialect: Validation Profile
version: 1.0

usage: Dialect to describe validations over RAML documents
uses:
  dlib: dialect_lib.raml
nodeMappings:
  functionConstraintNode:
      !extends: dlib.functionConstraintNode
