asyncapi: 2.6.0
channels:
  first-channel:
    subscribe:
      message:
        # all PayloadModel from here
        contentType: "test"
        schemaFormat: "application/vnd.apache.avro;version=1.9.0"
        payload:
