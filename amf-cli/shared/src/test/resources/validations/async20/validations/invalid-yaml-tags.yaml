asyncapi: 2.0.0

info:
  title: Signup service example (internal)
  !!firstInvalid version: !!secondInvalid 0.1.0

channels:
  /user/signedup:
    subscribe:
      message:
        payload:
          type: string
          minLength: 4
          examples: !finalInvalid [
            "lalaala"
          ]

