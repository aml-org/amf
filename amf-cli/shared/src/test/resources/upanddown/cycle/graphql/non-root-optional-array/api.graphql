type Customer {
  name: String!
  customerId: String!
}

type Product {
  name: String!
  productId: String!
}

type Query {
  customers(count: Int): [Customer!]
  products(count: Int): [Product!]
}

schema {
  query: Query
}