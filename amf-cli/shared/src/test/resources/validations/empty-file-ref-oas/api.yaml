openapi: "3.0.0"

info:
  version: 1.0.0
  title: test-empty-ref

paths:
  /e:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: empty-file.yaml#/components/schemas/mySchema