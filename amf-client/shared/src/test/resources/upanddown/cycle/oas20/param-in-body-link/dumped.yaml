swagger: "2.0"
consumes:
  - application/json
produces:
  - application/json
  - application/xml
paths:
  /resource:
    post:
      parameters:
        -
          $ref: "#/parameters/bp"
      responses: {}
parameters:
  bp:
    in: body
    schema:
      type: object
    name: bp
  fp:
    name: fp
    in: formData
    type: object
info:
  title: API
  version: "1.0"
