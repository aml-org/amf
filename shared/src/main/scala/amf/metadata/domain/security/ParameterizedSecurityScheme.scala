package amf.metadata.domain.security

import amf.metadata.domain.DomainElementModel

// TODO this class should point to a security scheme and store which scopes to pass as parameters.
class ParameterizedSecurityScheme extends DomainElementModel {
  override def fields = ???

  override val `type` = ???
}
