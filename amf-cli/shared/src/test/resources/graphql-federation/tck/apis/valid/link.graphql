schema @MyDirective @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@requires", "@provides", "@external", { name: "@tag", as: "@mytag" }, "@extends", "@shareable", "@inaccessible", "@override"]){
    query: Query
}

type Query {
    text: String
}

directive @MyDirective on SCHEMA