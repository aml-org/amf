swagger: "2.0"
paths:
  /resource:
    post:
      consumes:
        - application/json
      parameters:
        -
          name: OperationFormParam
          in: formData
          description: some body parameter
          type: string
      responses: {}
info:
  title: API
  version: "1.0"
