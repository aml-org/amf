asyncapi: '2.0.0'
info:
  title: Mqtt message binding
  version: '1.0.0'

channels:
  some-channel:
    publish:
      message:
        bindings:
          mqtt:
            bindingVersion: 0.1.0

  other-channel:
    publish:
      message:
        bindings:
          mqtt:
            bindingVersion: 0.2.0