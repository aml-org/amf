swagger: "2.0"
info:
  title: Simple API overview
  version: 2.0.0
paths:
  /pets:
    post:
      summary: Add a new pet
      description: Optional description in *Markdown*
      produces:
        - application/json
      responses:
        "200":
          description: OK
      parameters:
        -
          name: parameterName
          required: true
          type: string
          in: formData
