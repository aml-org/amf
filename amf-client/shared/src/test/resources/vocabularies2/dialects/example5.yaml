#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  nodeB:
    classTerm: v2.A
    mapping:
      za:
        propertyTerm: v2e.a
        range: nodeA
  nodeA:
    classTerm: v2e.A
    mapping:
      a:
        propertyTerm: v2.a
        range: string
documents:
  root:
    encodes: nodeA
    declares:
      DecB: nodeB
uses:
  v2: vocab2.yaml
external:
  v2e: http://test.com/external#