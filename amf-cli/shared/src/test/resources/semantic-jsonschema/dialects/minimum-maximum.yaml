#%Dialect 1.0
nodeMappings:
  Person:
    mapping:
      name:
        range: string
        mandatory: true
      age:
        range: integer
        mandatory: true
        minimum: 0.0
        maximum: 150.0
external:
  ns0: http://test.com/vocab#
dialect: amf-json-schema-generated-dialect
version: "1.0"
documents:
  root:
    encodes: Person
