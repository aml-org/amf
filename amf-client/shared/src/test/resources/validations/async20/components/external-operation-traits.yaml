asyncapi: 2.0.0
info:
  title: anOperation
  version: 1.0.0
channels:
  someChannel:
    publish:
      operationId: something
      traits:
        - $ref: 'external-traits.json#/firstOperationTrait'