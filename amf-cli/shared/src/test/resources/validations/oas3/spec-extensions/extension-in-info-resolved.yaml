openapi: 3.0.0
info:
  title: API
  version: 1.0.0
  x-extension:
    prop: value
paths:
  /path:
    get:
      responses:
        "200":
          description: A response.
          content:
            application/json:
              schema:
                type: object
