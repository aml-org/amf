asyncapi: 2.0.0
info:
  title: API
  version: "1.0"

components:
  schemas:
    UnknownType:
      type: string
channels:
  users/signup:
    subscribe:
      message:
        schemaFormat: application/vnd.rai.raml;version=1.0
        payload:
          $ref: external-invalid-library.raml#/types/InvalidType
