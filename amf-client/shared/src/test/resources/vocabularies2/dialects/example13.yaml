#%Dialect 1.0
dialect: Test
version: "1.0"
nodeMappings:
  Local:
    classTerm: v3.Local
    mapping:
      za:
        propertyTerm: v3.za
        range: lib.nodeB
uses:
  lib: mappings_lib.yaml
external:
  v3: http://test.com/v3#
documents:
  root:
    encodes: Local
