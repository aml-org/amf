openapi: 3.0.0
info:
  title: Party Customer Process API
  version: "1.0"
paths:
  /test:
    get:
      responses:
        "200":
          description: ""
      security:
        -
          oauth_2_0:
            - edit:customer
components:
  securitySchemes:
    oauth_2_0:
      flows:
        clientCredentials:
          tokenUrl: https://vis-dev.eu.auth0.com/oauth/token
          scopes:
            read:customer: "null"
            edit:customer: "null"
      type: oauth2
