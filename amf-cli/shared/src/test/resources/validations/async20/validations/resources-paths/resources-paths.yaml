asyncapi: '2.0.0'
info:
  title: Correlation ID Example
  version: '1.0.0'

channels:
  /user:
    subscribe:
      message:
        payload:
          type: string
  /user:
    subscribe:
      message:
        payload:
          type: string
  /user/{param}:
    parameters:
      param:
        schema:
          type: string
    subscribe:
      message:
        payload:
          type: string
  /user/{param1}:
    parameters:
      param1:
        schema:
          type: string
    subscribe:
      message:
        payload:
          type: string