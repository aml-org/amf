asyncapi: '2.0.0'
info:
  title: aTitle
  version: '1.0.0'
channels:
  aChannel:
    publish:
      message:
        correlationId:
          $ref: './libraries/first-lib.json#/definitions/correlationId'